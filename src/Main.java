/**
 * // -------------------------------------------------------------------------
 * /** Creates an AppRunner and starts the main menu loop.
 * 
 * @author scottpost
 * @version Sep 20, 2026
 */
public class Main
{
    /**
     * Starts the application
     * 
     * @param args
     *            command-line arguments (not used)
     */
    public static void main(String[] args)
    {
        AppRunner app = new AppRunner();
        app.run();
    }
}
